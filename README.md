# Datastar Streaming + Fat Morph

A tiny demo that streams a live-updating view at ~10fps and records which client triggered each increment.

## Run

```bash
air
```

Then open `http://localhost:8080` in your browser.
